spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: 'TRUE'
    hibernate:
      ddl-auto: update
    open-in-view: 'false'
  mvc:
    hiddenmethod:
      filter:
        enabled: 'true'
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    password: ${DB_PASSWORD}
    username: ${DB_USERNAME}
    url: jdbc:mysql://localhost:3306/POfB?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true&serverTimezone=UTC
logging:
  level:
    org:
      hibernate:
        type:
          descriptor: TRACE
        SQL: DEBUG
      nodeValue: WARN
    blog: WARN
server:
  port: 8080
apofb:
  mail:
    send-emails: true
    host: smtp.mailgun.org
    port: 587
    username: postmaster@sandbox9378a006e54c425999e310fe3d09c067.mailgun.org
    password: 3302f14a7b55014f8841d8ce06f4e29f-1c7e8847-7f1366c4
    from-email: example@axample.com
    from-title: Product Notifier
    transport-protocol: smtp
    smtp-auth: true
    smtp-starttls-enable: true
    smtp-ssl-enable: false
    debug: true
